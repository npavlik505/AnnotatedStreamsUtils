Bootstrap: localimage
From: nv.sif

%post
	apt-get update -y
	apt-get install curl libssl-dev pkg-config -y
	curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
	export PATH="$PATH":"$HOME/.cargo/bin"

	cargo
